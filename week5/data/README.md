This data directory is used for the week5 project. This README is here so that the directory can be added to Github.
When populated by the notebook it has the following structure:

```
data/
├── alignments/
├── gatk_output/
├── reads/
└── ref_genome/
```
Every time you run the notebook, the data directory will be (re)populated with the necessary files for the analysis that occurs in the notebook. Any files